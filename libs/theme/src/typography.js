const MUKTA = 'Mukta'

export const typography = {
  heading1: {
    fontFamily: `${MUKTA}`,
    fontSize: 45,
    fontWeight: 800,
  },
  heading2: {
    fontFamily: `${MUKTA}`,
    fontSize: 28,
    fontWeight: 700,
  },
  heading3: {
    fontFamily: `${MUKTA}`,
    fontSize: 24,
    fontWeight: 700,
  },
  heading4: {
    fontFamily: `${MUKTA}`,
    fontSize: 18,
    fontWeight: 700,
  },
  heading5: {
    fontFamily: `${MUKTA}`,
    fontSize: 16,
    fontWeight: 700,
  },
  bodyBold1: { fontFamily: `${MUKTA}`, fontSize: 15, fontWeight: 500 },
  display1: {
    fontFamily: `${MUKTA}`,
    fontSize: 22,
    fontWeight: 400,
  },
  display2: {
    fontFamily: `${MUKTA}`,
    fontSize: 18,
    fontWeight: 400,
  },
  display3: {
    fontFamily: `${MUKTA}`,
    fontSize: 16,
    fontWeight: 400,
  },
  display4: {
    fontFamily: `${MUKTA}`,
    fontSize: 13,
    fontWeight: 400,
  },
  display5: {
    fontFamily: `${MUKTA}`,
    fontSize: 12,
    fontWeight: 400,
  },
  body1: {
    fontFamily: `${MUKTA}`,
    fontSize: 16,
    fontWeight: 300,
  },
  body2: {
    fontFamily: `${MUKTA}`,
    fontSize: 13,
    fontWeight: 300,
  },
}
